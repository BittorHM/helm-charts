apiVersion: v1
kind: Service
metadata:
  name: {{ include "firstapp1.fullname" . }}
  labels:
    {{- include "firstapp1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firstapp1.selectorLabels" . | nindent 4 }}
