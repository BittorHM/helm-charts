apiVersion: v1
kind: Service
metadata:
  name: {{ include "cnpg-chart.fullname" . }}
  labels:
    {{- include "cnpg-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cnpg-chart.selectorLabels" . | nindent 4 }}
