apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: myapp
  namespace: myapp
spec:
  rules:
  - host: myapp.com
    http:
      paths:
      - backend:
          serviceName: nginx-svc 
          servicePort: 80
